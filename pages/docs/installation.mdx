---
title: Installation of ConvertFast UI
description: ConvertFast UI is a React + TypeScript library that helps you create a landing page with customizable sections and components. Learn how to install ConvertFast UI in your project.
---

# Installation

Code generated by convertfast-ui is based on shadcn-ui, all code are React + TypeScript, so make sure your project is set up with TypeScript and React.

A import alias is required to use the generated code, you can add the following to your `tsconfig.json`:

```json
{
  "compilerOptions": {
    "paths": {
      "@/*": ["./*"]
    }
  }
}
```