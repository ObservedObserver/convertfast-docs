# Examples

import { Card, CardContent, CardHeader, CardFooter, CardTitle } from '@/components/ui/card'
import { Button } from '@/components/ui/button'
import Link from 'next/link'

<div className="grid grid-cols-3 gap-2">
    <Card className="flex flex-col">
        <CardHeader>
            <CardTitle>Graphic Walker</CardTitle>
        </CardHeader>
        <CardContent>
            <p>React component for building an interactive data visualization app, lite open source alternative to tableau.</p>
        </CardContent>
        <CardFooter className="mt-auto">
            <Button className="w-full" asChild>
                <Link href="/examples/graphic-walker">View Example</Link>
            </Button>
        </CardFooter>
    </Card>
    <Card className="flex flex-col">
        <CardHeader>
            <CardTitle>Zeabur</CardTitle>
        </CardHeader>
        <CardContent>
            <p>PaaS platform to deploy apps with one click.</p>
        </CardContent>
        <CardFooter className="mt-auto">
            <Button className="w-full" asChild>
                <Link href="/examples/zeabur">View Example</Link>
            </Button>
        </CardFooter>
    </Card>
    <Card className="flex flex-col">
        <CardHeader>
            <CardTitle>Lab2</CardTitle>
        </CardHeader>
        <CardContent>
            <p>The AI Assistant for building data apps.</p>
        </CardContent>
        <CardFooter className="mt-auto">
            <Button className="w-full" asChild>
                <Link href="/examples/lab2">View Example</Link>
            </Button>
        </CardFooter>
    </Card>
</div>
